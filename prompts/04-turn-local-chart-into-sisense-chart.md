Take one existing local chart component and replace it with a Compose SDK chart.

Guidelines:
- Do not touch unrelated UI.
- Keep chart title and storytelling intent unchanged.
- If Sisense config is missing, render a non-crashing fallback message.
